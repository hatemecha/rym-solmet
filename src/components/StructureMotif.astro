---
import motif from "../assets/brand/contour-lines.svg";

interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<div
  class:list={["structure-motif", className]}
  data-motif
  style={`--motif-image: url("${motif.src}")`}
  aria-hidden="true"
></div>
