---
import mesh from "../assets/patterns/warped-mesh.svg?url";
interface Props { variant: "grid" | "dots" | "diagonal"; }
const { variant } = Astro.props;
---

<div class={`surface-pattern surface-pattern--${variant}`} style={{ backgroundImage: `url(${mesh})` }} data-surface-pattern={variant} aria-hidden="true"></div>

<style>
  .surface-pattern {
    position: absolute;
    inset: 0;
    z-index: 0;
    pointer-events: none;
    background-repeat: repeat;
    background-size: 900px 900px;
    opacity: 0.13;
  }
  .surface-pattern--grid {
    background-position: center 35%;
    mask-image: radial-gradient(ellipse at 12% 55%, #0003, #0008 45%, #000 85%);
  }
  .surface-pattern--dots {
    filter: invert(1);
    opacity: 0.16;
    background-position: 38% -180px;
    background-size: 1050px 1050px;
    mask-image: linear-gradient(#0004, #000 420px);
  }
  .surface-pattern--diagonal {
    background-position: 68% 80%;
    background-size: 800px 800px;
    mask-image: linear-gradient(#000, #0005 32%, #0005 75%, #000);
  }
  @media (max-width: 47.999rem) {
    .surface-pattern { background-size: 620px 620px; }
    .surface-pattern--dots { background-size: 740px 740px; }
    .surface-pattern--diagonal { background-position: 45% center; }
  }
</style>
